---
date: 2026-08-15
weekday: Saturday
status: final
tags: [work, backend, auth, supabase, onboarding, health]
mood: 4
energy: 3
generated_from: drafts/draft_2026-08-15.md
---

## Summary

A work-heavy Saturday focused on two tracks: fixing a production-facing auth
bug in the admin panel, and researching row-level security for a side
project. Both were technical wins — the bug fix was deployed to staging and
the RLS research produced a concrete implementation plan. A short meeting
with Dina set a clear direction for the mobile onboarding flow, which
simplifies to three steps. The day ended with a light gym session.

## Accomplishments

- Fixed the JWT expiry check bug in the admin panel and deployed the fix to staging
- Researched Supabase row-level security for the side project (~1h run-through of `pg` policies)
- Agreed with Dina on a simplified 3-step mobile onboarding flow

## Learnings

- Supabase RLS policies evaluate per-row with the role's `USING` clause before `WITH CHECK` — plan both for `INSERT`/`UPDATE`. **Takeaway:** write the `USING` and `WITH CHECK` clauses separately when designing policies.
- A 6-month-old token bug hid behind a wrong `exp` comparison, not a missing check. **Takeaway:** when debugging auth, re-read the comparison direction first, not the surrounding logic.

## Blockers / Next steps

- [ ] Write onboarding flow doc (3-step version) following Dina's direction
- [ ] Ask backend team about refresh token rotation support

## Day in detail

#### Activity: Fix admin panel auth bug (09:30-11:00)

- **Situation:** users reported random logouts in the admin panel; reports started appearing a week after the last auth change.
- **Task:** find and fix the cause of intermittent session loss.
- **Action:** traced the JWT verification path, found the `exp` check compared against a timestamp in the wrong unit; corrected the comparison and added a unit test that reproduces the off-by-factor scenario.
- **Result:** fix deployed to staging; logs no longer show forced re-auths during the test window.
- **Reflection:** the bug survived reviews because the test only mocked a "valid" token — future auth changes should include an expiry-boundary test.

#### Activity: Supabase RLS research (13:00-14:00)

- **Situation:** side project stores per-user data in Postgres and currently trusts app-level filtering.
- **Task:** determine whether row-level security can enforce tenant isolation at the database level.
- **Action:** read Supabase docs on policies and permission-aware views, sketched `USING`/`WITH CHECK` policies for the `projects` table, prototyped one policy in a scratch project.
- **Result:** confirmed the approach works; plan is to add policies before opening the project to more users.
- **Reflection:** a prototype took less time than expected — worth doing before writing the migration.

#### Activity: Onboarding meeting with Dina (15:00-15:30)

- **Situation:** mobile onboarding had 7 steps and drop-off was high on step 4.
- **Task:** align on a simpler onboarding direction.
- **Action:** walked through current metrics with Dina; she proposed a 3-step flow (account, preferences, invite) which we agreed to adopt; I took ownership of the feature doc.
- **Result:** agreed scope + owner; next step is the written doc.
- **Reflection:** I should have proposed cutting steps earlier — fewer steps were obvious from the drop-off data.

#### Activity: Gym (18:30-19:00)

- **Situation:** no exercise this week, energy was low in the afternoon.
- **Task:** maintain the twice-a-week routine.
- **Action:** 30-minute session, upper-body focus.
- **Result:** 30 min logged; happened as scheduled.
- **Reflection:** scheduling a fixed post-work slot made it automatic.